const FlagCaret = ({name, color}) => {
	return (
		<span className='text-secondary -mx-1 relative animate-blink'>
			<span className="absolute top-0 text-xs ml-1 -mt-1 bg-secondary bg-opacity-50 text-primary">
        team_name
      </span>
			|
		</span>)};

export default FlagCaret;